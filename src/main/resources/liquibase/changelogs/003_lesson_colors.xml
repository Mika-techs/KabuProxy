<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.2.xsd">

    <changeSet id="003-user-lesson-colors" author="mik">
        <createTable tableName="user_lesson_color">
            <column name="user_id" type="BIGINT">
                <constraints primaryKey="true" primaryKeyName="pk_user_lesson_color" foreignKeyName="fk_user_lesson_color_settings" references="user_settings(user_id)" deleteCascade="true"/>
            </column>
            <!-- lesson.subject as crawled -->
            <column name="subject" type="VARCHAR(100)">
                <constraints primaryKey="true" primaryKeyName="pk_user_lesson_color"/>
            </column>
            <!-- lesson.teacher, empty = the whole subject; lookup: subject + teacher, subject, accent colour -->
            <column name="teacher" type="VARCHAR(100)" defaultValue="">
                <constraints primaryKey="true" primaryKeyName="pk_user_lesson_color"/>
            </column>
            <!-- #rrggbb -->
            <column name="color" type="VARCHAR(7)">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <!-- subjects/teachers are keys: "Deutsch" and "deutsch" must not collide under the case-insensitive server collation -->
        <modifySql dbms="mysql">
            <append value=" CHARACTER SET utf8mb4 COLLATE utf8mb4_bin"/>
        </modifySql>
    </changeSet>
</databaseChangeLog>
