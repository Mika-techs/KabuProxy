# Intentionally empty: German lives in messages.properties (the root bundle). This file only exists so a lookup for "de"
# never falls back to the JVM's default locale (which could be English) and picks messages_en.properties instead.
